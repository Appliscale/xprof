language: erlang
branches:
    only:
        - ct_timing_issues
otp_release:
    - 20.0
    - 19.3
    - 18.3
    - 17.5
    - R16B03-1
cache:
    directories:
        - $HOME/.cache/rebar3
        - priv/node_modules
install:
    - ./rebar3 deps
    - nvm install 6.2 && nvm use 6.2
    - make bootstrap_front_end
script:
    - make test
